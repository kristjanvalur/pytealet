# _tealet C Extension

This directory contains the C extension module for tealet.

## Structure

- `_tealet.c` - Main C extension module implementation
- `libtealet/` - (To be added) Git submodule/subtree containing libtealet from GitHub

## Building

The C extension is configured in the project's `pyproject.toml` and will be built automatically during installation.
